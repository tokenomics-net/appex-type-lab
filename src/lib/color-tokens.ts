/**
 * color-tokens.ts
 *
 * Intentionally emptied. The HSL-delta color tuning system has been removed
 * in favor of native <input type="color"> per role in the simplified lab.
 * This file is retained to avoid import errors from any stale references
 * during the migration; it exports nothing.
 */

export {};
